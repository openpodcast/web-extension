{
  "name": "web-extension",
  "version": "1.4.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start:firefox": "web-ext run -t firefox-desktop --source-dir ./src/",
    "start:chrome": "web-ext run -t chromium --source-dir ./src/",
    "build": "web-ext build --overwrite-dest --source-dir ./src/"
  },
  "devDependencies": {
    "web-ext": "^7.2.0"
  }
}